# ARCHITECTURE.md — IGBZ (پلتفرم بومی/مستقل)
## سند مرجع معماری — نسخهٔ ۳: توقف موقت مهاجرت به بومی، ادامه روی nopCommerce

> **تصمیم راهبردی جدید (نسخهٔ ۳ — بالاترین اولویت سند):** مهاجرت کامل به پلتفرم بومی (.NET
> ۹/MongoDB، توضیح‌داده‌شده در نسخهٔ ۲ همین سند) **به تعویق افتاد**. تا زمانی که IGBZ به یک برند
> تجاری واقعی و اثبات‌شده در بازار تبدیل نشود، پروژه **روی همان nopCommerce 4.90.6 ادامه پیدا
> می‌کند** و پلاگین‌های موجود (`Nop.Plugin.Misc.MultiTenantStores`, `Nop.Plugin.Misc.InstagramAssistant`,
> `Nop.Plugin.Misc.MasterSiteHub`, `Nop.Plugin.Api`) از حالت «فقط مرجع منطق» خارج و به **کد واقعی
> Production** تبدیل می‌شوند. دلیل این تصمیم: ریسک بازنویسی کامل زیرساخت پیش از اثبات بازار برای
> یک کسب‌وکار نوپا توجیه ندارد؛ عاقلانه‌تر است ابتدا محصول با پایه فعلی (nopCommerce) به بازار
> برسد و بلوغ تجاری پیدا کند.
>
> **پیامدهای این تصمیم روی این سند:**
> - بخش‌های ۳ تا ۱۸ (استک فنی بومی، مدل داده MongoDB، موتور سفارش/تخفیف/پرداخت بومی) اکنون
>   **نقشهٔ راه آینده** هستند، نه کار جاری — در بخش «۲۲. وضعیت فعلی اجرا» (انتهای سند) به‌روز نگه
>   داشته می‌شوند.
> - سند جدید `NOPCOMMERCE-EXECUTION.md` (این مخزن) اکنون سند فنی **جاری و عملیاتی** است: پوشش کامل
>   ممیزی کد واقعی روی سورس رسمی nopCommerce 4.90.6، اصلاح تمام ناسازگاری‌های API، و راهنمای
>   استقرار واقعی.
> - این تصمیم در هر لحظه با بلوغ کافی برند قابل بازنگری است؛ نسخهٔ ۲ همین سند (بخش‌های معماری
>   بومی) دست‌نخورده باقی می‌ماند تا در آن زمان مجدداً مرجع اجرا شود.

> **وضعیت این سند:** منبع حقیقت فعلی و نهایی پروژه. جایگزین کامل هر دو سند قبلی می‌شود:
> (۱) `ARCHITECTURE.md` اصلی (مبتنی بر nopCommerce)، (۲) `ARCHITECTURE-NATIVE.md` (پیش‌نویس اول
> معماری Native که تصمیمات بخش ۱۲ آن هنوز باز بود). تمام تصمیمات باز در این نسخه پاسخ داده
> شده‌اند (بخش ۱۹) تا سند بتواند مرجع کامل و بدون‌ابهام برای شروع پیاده‌سازی فاز ۱ باشد.
>
> **ورودی‌های این بازنویسی:**
> 1. مرور کامل `ARCHITECTURE-NATIVE.md` (تصمیم جدایی از nopCommerce، مدل کسب‌وکار، فازبندی).
> 2. ممیزی کد به کد مخزن `nopcommerce-plugin-generator` (اپ ژنراتور + پلاگین‌های مرجع C#)، که
>    نتایج آن در بخش ۱۸ («استانداردهای مهندسی برگرفته از ممیزی کد») به‌صورت قواعد الزام‌آور برای
>    پیاده‌سازی بومی جدید تبدیل شده‌اند — چون همان دسته اشکالات (پاسخ همیشه‌موفق درگاه پرداخت،
>    داده‌های Hardcode جای فراخوانی واقعی، سرویس‌های ارجاع‌داده‌شده ولی هرگز تعریف‌نشده) در صورت
>    تکرار در کدبیس بومی به‌مراتب خطرناک‌تر خواهند بود چون آن کد قرار است واقعاً Production شود.

**آخرین به‌روزرسانی:** تدوین نسخهٔ کامل و نهایی سند معماری با پاسخ به تمام تصمیمات باز.

---

## فهرست مطالب
1. مدل کسب‌وکار و برند
2. خلاصهٔ ممیزی کد مرجع (چرا این سند اهمیت دارد)
3. استک فنی نهایی
4. معماری کلی و نمودار جریان
5. چندمستأجری از پایه (Multi-Tenancy)
6. مدل داده (Schema کامل MongoDB)
7. موتور سفارش (Order Engine)
8. موتور تخفیف (Discount Engine) — تصمیم نهایی
9. لایهٔ پرداخت و دفترکل تراکنش
10. چارچوب یکپارچه‌سازی بیرونی (Integration Provider Framework)
11. سایت مادر، ویزارد و Provisioning
12. فروشگاه تننت، Web API و اپ‌های موبایل
13. دستیار اینستاگرام و کیف‌پول
14. استودیوی هوش مصنوعی محتوا
15. سرویس آموزش (LMS)
16. امنیت و مدیریت کلید/راز
17. رصدپذیری، تاب‌آوری و تست
18. استانداردهای مهندسی برگرفته از ممیزی کد (الزامی)
19. تصمیمات باز قبلی — پاسخ نهایی
20. فازبندی اجرایی نهایی
21. تاریخچهٔ تصمیمات (Changelog)

---

## ۱. مدل کسب‌وکار و برند

بدون تغییر نسبت به سند قبلی: پلتفرم **IGBZ** («دستیار کسب‌وکارهای اینستاگرامی»، عمداً بدون کلمهٔ
Instagram در برند به‌دلیل ریسک نقض علامت تجاری Meta) که سه ابزار می‌فروشد:

| # | سرویس | وضعیت در این سند |
|---|---|---|
| 1 | فروشگاه‌ساز اختصاصی (وب + موتور تجارت بومی، MongoDB/.NET 9) | مرجع کامل طراحی — بخش‌های ۵ تا ۹ |
| 2 | اپ ادمین و اپ مشتریان (Flutter) روی Web API بومی | بخش ۱۲ |
| 3 | دستیار هوشمند اینستاگرام + رشد فالوور + LMS | بخش‌های ۱۳ تا ۱۵ |

**ساختار مالکیت/عملیات:** شرکت ثبت‌شدهٔ واقعی در قبرس، بدون هیچ روش دور زدن تحریم (هویت جعلی،
پنهان‌سازی IP). این قاعده تغییرناپذیر است و مبنای تصمیم‌گیری بخش ۱۹.۲ (پرداخت بین‌المللی) است.

مدل پلن‌های اشتراکی (Bronze/Silver/Gold Pro/Diamond) از تحقیقات فاز قبلی معتبر می‌ماند و باید در
جدول `TenantPlans` (بخش ۶.۹) به‌صورت داده، نه کد Hardcode، تعریف شود — دقیقاً برخلاف الگویی که در
کد مرجع دیده شد (نمونه: `MonthlyRecurringRevenueToman = activeStoresCount * 850000`، یک فرمول
جعلی که همهٔ تننت‌ها را با یک نرخ ثابت فرض می‌کرد؛ رجوع به بخش ۲).

---

## ۲. خلاصهٔ ممیزی کد مرجع (چرا این سند اهمیت دارد)

پیش از نوشتن معماری جدید، کل مخزن کد مرجع (پلاگین‌های nopCommerce + اپ ژنراتور React) خط‌به‌خط
بررسی شد. نتیجه: **منطق دامنهٔ داخلی** (تشخیص Tenant، فیلترهای امنیتی جداسازی Store، نگاشت
دامنه) درست و قابل‌اعتماد پیاده‌سازی شده بود؛ اما تقریباً **هر سرویس یکپارچه‌سازی بیرونی** یکی از
سه الگوی معیوب زیر را داشت:

| الگوی معیوب مشاهده‌شده | نمونهٔ واقعی از کد | ریسک |
|---|---|---|
| **موفقیت جعلی بدون فراخوانی واقعی** | `ParbadPaymentService.VerifyPaymentAsync` همیشه `IsSuccess = true` برمی‌گرداند، مستقل از پاسخ واقعی بانک | **بحرانی** — تحویل رایگان کالا به هرکسی که فقط URL بازگشت را صدا بزند |
| **داده‌ی ثابت/Hardcode به‌جای واقعی** | «ترجمهٔ خودکار محصول» همیشه رشتهٔ ثابت «Genuine Leather Shoes» برمی‌گرداند، مستقل از محصول ورودی؛ فید ترب دو محصول نمونهٔ ثابت داشت | بالا — داده نادرست به مشتری/مارکت‌پلیس بیرونی |
| **سرویس ارجاع‌داده‌شده ولی هرگز تعریف‌نشده** | `IFcmService`، `AuthorizedTenantOwnerApiController`، `ITenantPlanService` در چند کنترلر Inject/فراخوانی می‌شدند بدون این‌که پیاده‌سازی یا حتی Interface آن‌ها در جایی از مخزن وجود داشته باشد | بحرانی — کل لایهٔ Admin API/Billing اصلاً Compile نمی‌شد |

این یافته‌ها به دو شکل در این سند اثر گذاشتند:
1. **بخش ۱۸** آن‌ها را به قواعد مهندسی الزام‌آور (Definition of Done) برای پیاده‌سازی بومی جدید
   تبدیل می‌کند — هدف: تکرارنشدن این کلاس از باگ در کدی که واقعاً Production می‌شود.
2. **بخش ۹ و ۱۰** به‌صورت صریح یک لایهٔ ماندگاری (Ledger/Credential Store) را از روز اول در
   طراحی داده جای می‌دهند، دقیقاً چون نبود چنین لایه‌ای علت ریشه‌ای دو مورد از سه الگوی بالا بود.

> نکتهٔ مهم: کد مرجع (`Plugins/`) به‌عنوان مبنای منطق Edge Case برای همین سند اصلاح شد (نمونه:
> `ParbadPaymentService`، `LmsAndVideoSecurityService`، `TenantPlanService` بازنویسی و تکمیل
> شدند) تا در صورت نیاز به مرور دوبارهٔ منطق، نسخهٔ درست و نه فریب‌کارانه در دسترس باشد. این کار
> تغییری در تصمیم اصلی (رهاکردن nopCommerce) ایجاد نمی‌کند.

---

## ۳. استک فنی نهایی

| لایه | انتخاب نهایی | دلیل |
|---|---|---|
| بک‌اند | **.NET 9 / C#**، معماری Clean/Vertical-Slice | تداوم دانش تیم |
| دیتابیس اصلی | **MongoDB 7+** (Replica Set از روز اول، حتی در تک‌سرور، برای Transactions) | مدل سند تودرتوی طبیعی برای Order/Product؛ `findOneAndUpdate` اتمیک برای موجودی |
| کش/صف سریع | **Redis** (کش + Rate Limiting + Distributed Lock) | جداسازی Namespace بر اساس `tenantId` |
| صف پیام/Job | **RabbitMQ** برای رویدادهای Async بین‌سرویسی + **Hangfire (Mongo Storage)** برای Job‌های زمان‌بندی‌شده/تاخیری با داشبورد قابل‌رصد | پاسخ نهایی به بند باز قبلی؛ توضیح کامل در بخش ۱۹.۴ |
| فرانت‌اند وب | **Next.js (React، App Router)** برای سایت مادر و Storefront تننت‌ها | پاسخ نهایی به بند باز قبلی؛ توضیح در بخش ۱۹.۳ |
| اپ موبایل | **Flutter** (بدون تغییر) | ۲ اپ: ادمین، مشتریان |
| میزبانی ویدیو/VOD | سرویس‌های VOD ریالی ایرانی (آروان‌کلاد و مشابه)، HLS + Signed URL | بخش ۱۵.۲ |
| مانیتورینگ | OpenTelemetry (Trace/Metric/Log) + Grafana/Loki یا Elastic Stack | بخش ۱۷ |
| CI/CD | GitHub Actions/GitLab CI → Docker Image → Deploy (Docker Compose برای MVP، مهاجرت به Kubernetes/Nomad در فاز ۱۲ در صورت رشد) | بخش ۱۶ (زیرساخت) |

---

## ۴. معماری کلی و نمودار جریان

```
                        ┌─────────────────────────────┐
                        │   Next.js — سایت مادر (Landing/Wizard)  │
                        └───────────────┬─────────────┘
                                        │ HTTPS/REST
┌──────────────┐     ┌──────────────────▼───────────────────┐     ┌───────────────┐
│ Next.js       │────▶│         Web API واحد (.NET 9)          │◀────│ Flutter Admin │
│ Storefront    │     │  Auth · Tenant Resolver Middleware ·   │     │ Flutter Buyer │
│ (هر تننت)     │     │  Order/Discount/Tax Pipeline ·         │     └───────────────┘
└──────────────┘     │  Integration Provider Framework        │
                       └───────┬───────────────┬──────────────┘
                               │               │
                     ┌─────────▼───┐   ┌───────▼─────────┐
                     │  MongoDB     │   │  RabbitMQ (Bus)  │
                     │ (per-tenant  │   │  + Hangfire      │
                     │  scoped)     │   │  (Scheduled Jobs)│
                     └──────────────┘   └───────┬──────────┘
                                                 │
                     ┌───────────────────────────▼──────────────────────────┐
                     │  Worker Services (مصرف‌کنندگان صف)                    │
                     │  · Marketplace Sync (ترب/دیجی‌کالا/دیوار)             │
                     │  · Payment Verification Retry                        │
                     │  · AI Content Studio (تصویر/ویدیو/صدا/ترجمه)         │
                     │  · Instagram Webhook Processor                       │
                     │  · Notification Dispatcher (SMS/Push/Email)          │
                     └───────────────────────────────────────────────────────┘
```

اصل کلیدی: **هیچ Controller مستقیماً یک API بیرونی را صدا نمی‌زند اگر آن عملیات می‌تواند Async
باشد.** فقط عملیات‌های Read/Verify هم‌زمان (مثلاً «آیا زیردامنه آزاد است؟») مستقیم صدا زده
می‌شوند؛ Sync/Publish/Notify از صف عبور می‌کنند تا Retry/Idempotency تضمین شود (رجوع به الگوی
معیوب اول در بخش ۲ — فراخوانی هم‌زمان بدون صف دقیقاً همان‌جایی بود که «فراخوانی واقعی» با
«فراخوانی جعلی» قابل‌اشتباه‌گرفتن بود، چون هیچ لاگ/Retry قابل‌رصدی وجود نداشت).

---

## ۵. چندمستأجری از پایه (Multi-Tenancy by Design)

- هر Document در هر Collection (به‌جز چند Collection سطح پلتفرم مثل `TenantPlans`) دارای فیلد
  اجباری `tenantId` است.
- `ITenantScopedRepository<T>`: هیچ Query‌ای بدون `tenantId` قبول نمی‌کند — قفل کامپایل‌تایم از
  طریق یک Generic Constraint + Runtime Guard که در صورت فراخوانی بدون Context، Exception پرتاب
  می‌کند (نه یک فیلتر قابل‌فراموش‌شدن). این دقیقاً معادل رفتار خوبی بود که در
  `TenantAdminScopeFilter` کد مرجع دیده شد و باید حفظ شود، منتها این‌بار در سطح Repository پایه،
  نه فقط یک Filter روی Controller.
- `tenantId` از JWT Claim (موبایل) یا Host Header (وب) استخراج و توسط Middleware تزریق می‌شود.
- سایت مادر یک «شبه‌تننت» با `tenantId = "platform"` است، از پایه در مدل جای گرفته.
- **تست جداسازی الزامی:** برای هر Collection جدید، یک تست خودکار باید تضمین کند که Query بدون
  `tenantId` واقعاً Exception می‌دهد (نه فقط نتیجهٔ خالی برمی‌گرداند — تفاوت مهم: نتیجهٔ خالی یعنی
  باگ ممکن است بی‌صدا رد شود، Exception یعنی باگ در توسعه فوراً دیده می‌شود).

---

## ۶. مدل داده (Schema کامل MongoDB)

### ۶.۱ `tenants`
```json
{
  "_id": "ObjectId",
  "tenantId": "string (slug, e.g. 'modstyle')",
  "displayName": "string",
  "planId": "ObjectId -> tenantPlans",
  "status": "Trial | Active | PastDue | Suspended | Cancelled",
  "domains": [{ "hostName": "string", "isPrimary": "bool", "isSslVerified": "bool" }],
  "createdOnUtc": "date",
  "settings": { "currency": "IRT", "taxRatePercent": 9 }
}
```

### ۶.۲ `products`
```json
{
  "_id": "ObjectId", "tenantId": "string",
  "name": "string", "slug": "string", "description": "string",
  "categoryIds": ["ObjectId"],
  "variants": [
    { "sku": "string", "attributes": {"color":"red","size":"L"},
      "priceToman": "decimal", "oldPriceToman": "decimal|null",
      "stockQuantity": "int", "reservedQuantity": "int" }
  ],
  "images": ["url"], "isPublished": "bool", "isDigital": "bool",
  "downloadRef": "ObjectId|null"
}
```

### ۶.۳ `orders`
```json
{
  "_id": "ObjectId", "tenantId": "string", "customerId": "ObjectId",
  "status": "Pending|Paid|Processing|Shipped|Delivered|Cancelled|Returned",
  "items": [{ "productId":"ObjectId","sku":"string","qty":"int","unitPriceToman":"decimal" }],
  "pricing": {
    "subTotalToman":"decimal","discountToman":"decimal","taxToman":"decimal",
    "shippingToman":"decimal","grandTotalToman":"decimal",
    "appliedCouponCodes":["string"]
  },
  "payment": { "gateway":"string","trackingNumber":"string","refId":"string|null" },
  "shipping": { "carrier":"string","trackingCode":"string|null","deliveryPin":"string|null" },
  "createdOnUtc":"date","statusHistory":[{"status":"string","atUtc":"date"}]
}
```
تغییر وضعیت فقط از طریق متدهای صریح Aggregate انجام می‌شود (`order.MarkAsPaid()`)، هرگز نوشتن
مستقیم فیلد `status`.

### ۶.۴ `paymentTransactionLedger` *(جدید — برگرفته از رفع باگ بحرانی بخش ۲)*
```json
{
  "_id":"ObjectId","tenantId":"string","orderId":"ObjectId","gatewayName":"string",
  "trackingNumber":"string (unique index)","amountToman":"decimal",
  "state":"Requested|RedirectedToBank|VerifiedSuccess|VerifiedFailed",
  "bankRefId":"string|null","rawGatewayResponse":"string",
  "requestedOnUtc":"date","verifiedOnUtc":"date|null"
}
```
**قاعدهٔ سخت:** `trackingNumber` یک Unique Index دارد و تایید یک تراکنش با `state=VerifiedSuccess`
هرگز دوباره مبلغ را اعتبار نمی‌زند (جلوگیری از Replay — دقیقاً رفع همان باگ `ParbadPaymentService`).

### ۶.۵ `walletLedger` *(جدید — برگرفته از رفع باگ «موجودی کیف‌پول ثابت»)*
```json
{ "_id":"ObjectId","tenantId":"string","customerId":"ObjectId","amountToman":"decimal",
  "reason":"OrderCashback|InstagramDonation|ContestReward|AffiliateCommission",
  "referenceCode":"string|null","createdOnUtc":"date" }
```
موجودی کیف‌پول **همیشه** `SUM(amountToman)` این Collection است؛ هیچ فیلد «موجودی فعلی» در جای
دیگری کش/ذخیره نمی‌شود مگر به‌عنوان Read-Model محاسبه‌شدهٔ Idempotent با Rebuild خودکار.

### ۶.۶ `integrationCredentials` *(جدید — پاسخ به الگوی معیوب سوم در بخش ۲)*
```json
{ "_id":"ObjectId","tenantId":"string","providerKey":"string (e.g. 'parbad.zarinpal','digikala')",
  "apiKey":"string (encrypted at rest)","apiSecret":"string (encrypted at rest)",
  "endpointOverrideUrl":"string|null","isActive":"bool","isVerified":"bool",
  "createdOnUtc":"date","updatedOnUtc":"date" }
```
**قاعده:** هیچ سرویس یکپارچه‌سازی مجاز نیست بدون یک رکورد `isActive=true AND isVerified=true`
عملیات را «آغاز» تلقی کند؛ نبود کلید = خطای صریح، نه موفقیت خاموش.

### ۶.۷ `shoppingCartItems`, ۶.۸ `customers`, ۶.۹ `tenantPlans`, ۶.۱۰ `tenantStoreSubscriptions`
ساختار مشابه نمونه‌های استاندارد commerce؛ `tenantPlans` شامل `priceMonthlyToman`,
`priceYearlyToman`, `maxProductsAllowed`, `allowCustomDomain`, `allowDedicatedApp` است — این جدول
منبع واحد قیمت‌گذاری است، هیچ‌جای دیگر کد نباید قیمت پلن را Hardcode کند (رجوع به بخش ۲).

---

## ۷. موتور سفارش (Order Engine)

### ۷.۱ ماشین‌حالت
```
Pending → Paid → Processing → Shipped → Delivered
                ↘ Cancelled       ↘ Returned/Refunded
```

### ۷.۲ پایپ‌لاین محاسبه
```
SubTotal → Discounts (بخش ۸) → Tax (VAT ثابت) → Shipping → GrandTotal
```
هر مرحله یک `ICalculator<TIn,TOut>` مستقل، Unit-Testable، بدون وابستگی جانبی پنهان.

### ۷.۳ موجودی
مدل بر اساس Variant، رزرو اتمیک با `findOneAndUpdate` روی شرط `stock - reserved >= qty`. هنگام
Timeout سبد خرید (پیش‌فرض ۱۵ دقیقه)، یک Job زمان‌بندی‌شده (Hangfire) رزرو را آزاد می‌کند.

### ۷.۴ مالیات
نرخ ثابت VAT در سطح تننت (`tenant.settings.taxRatePercent`)، قابل‌ویرایش توسط مالک تننت، پیش‌فرض
مطابق نرخ قانونی جاری.

---

## ۸. موتور تخفیف (Discount Engine) — تصمیم نهایی

**تصمیم:** سطح ۲ در فاز ۱ پیاده می‌شود؛ معماری از روز اول برای ارتقا به سطح ۳ باز طراحی می‌شود
(نه بازنویسی، فقط افزودن).

- **سطح ۲ (فاز ۱، الزامی):** کد تخفیف (Coupon) با قوانین: درصدی یا مبلغ ثابت، حداقل مبلغ سبد،
  تاریخ انقضا، سقف تعداد استفاده (کلی و به‌ازای هر مشتری)، عدم ترکیب هم‌زمان دو کد.
- **سطح ۳ (فاز بعدی، طراحی آماده ولی غیرفعال):** `IDiscountRule` چندگانه با فیلد `priority` و
  `combinable: bool`؛ پایپ‌لاین محاسبه (بخش ۷.۲) از هم‌اکنون یک `List<AppliedDiscount>` می‌گیرد
  نه یک مقدار تکی، دقیقاً تا فعال‌سازی سطح ۳ نیازمند تغییر Schema نباشد.
- **چرخ‌وفلک شانس / تخفیف سبد رهاشده:** هر دو به‌عنوان **تولیدکنندهٔ کد تخفیف سطح ۲** پیاده
  می‌شوند (نه یک مسیر جدا)، تا منطق اعتبارسنجی کد در یک‌جا بماند.

**دلیل تصمیم:** فهرست ویژگی‌های درخواستی («کد تخفیف»، «تخفیف سبد رهاشده») حداقل سطح ۲ را الزامی
می‌کند؛ سطح ۳ در فاز اول ریسک/زمان توسعه را بدون تقاضای اثبات‌شده افزایش می‌دهد.

---

## ۹. لایهٔ پرداخت و دفترکل تراکنش

### ۹.۱ انتزاع مشترک
```csharp
public interface IPaymentGatewayService
{
    Task<PaymentRequestResult> RequestAsync(PaymentRequest request);
    Task<PaymentVerifyResult> VerifyAsync(string trackingNumber, decimal expectedAmountToman);
}
```

### ۹.۲ قاعدهٔ سخت تایید (Non-Negotiable)
برگرفته مستقیم از بخش ۲ (رفع باگ بحرانی): `VerifyAsync` **هرگز** بدون یکی از این سه شرط
`IsSuccess=true` برنمی‌گرداند:
1. فراخوانی HTTP واقعی به Endpoint تایید PSP انجام شده باشد **و**
2. پاسخ HTTP موفق (2xx) باشد **و**
3. مبلغ تایید‌شده در پاسخ PSP دقیقاً با مبلغ سفارش در `paymentTransactionLedger` برابر باشد.

هر تایید (موفق یا ناموفق) در `paymentTransactionLedger` ثبت می‌شود؛ تایید دوم روی همان
`trackingNumber` با `state=VerifiedSuccess` قبلی، بدون فراخوانی مجدد بانک، مستقیماً موفق
برگردانده می‌شود ولی **پول دوباره واریز نمی‌شود** (Idempotency).

### ۹.۳ درگاه‌های پشتیبانی‌شده
همان فهرست قبلی (الگوی Parbad: ملت/ملی/سامان/پارسیان/پاسارگاد/ایران‌کیش/آسان‌پرداخت/سپهر +
زرین‌پال/Pay.ir) + BNPL (اسنپ‌پی/تارا/دیجی‌پی، با استعلام اعتبار Real-time، نه فقط محاسبهٔ محلی
اقساط — رجوع به یافتهٔ «HttpClient تزریق‌شده ولی هرگز استفاده‌نشده» در بخش ۲).

### ۹.۴ ارزی/بین‌المللی — پیشنهاد فنی (نیازمند تایید حقوقی نهایی)
با توجه به قاعدهٔ ثابت «بدون دور زدن تحریم»، گزینهٔ فنی پیشنهادی **درگاه رمزارزی (NOWPayments/
CoinGate/Plisio، تسویه به USDT)** است، چون:
- نیازمند حساب پذیرندگی نزد یک پردازشگر کارت آمریکایی/اروپایی نیست (که خودش می‌تواند تننشن
  تحریمی ایجاد کند).
- شرکت قبرسی می‌تواند به‌صورت شفاف (نه پنهانی) به‌عنوان طرف قرارداد ثبت شود.

این هنوز یک **تصمیم فنی پیشنهادی** است، نه تصمیم نهایی تجاری/حقوقی — طبق درخواست صریح سند قبلی
مبنی بر برگزاری یک جلسهٔ تصمیم‌گیری جدا، این بند باید پیش از پیاده‌سازی فاز ۶ توسط مشاور حقوقی
شرکت قبرسی تایید شود (مقررات KYC/AML رمزارز در قبرس/اتحادیهٔ اروپا نیز باید بررسی شود). Stripe/
PayPal مستقیم فقط برای مشتریان خارج ایران، به‌عنوان مسیر موازی و بدون‌تداخل، در نظر گرفته می‌شود.

---

## ۱۰. چارچوب یکپارچه‌سازی بیرونی (Integration Provider Framework)

### ۱۰.۱ انتزاع پایه
```csharp
public interface IIntegrationProvider
{
    string ProviderKey { get; }
    IntegrationProviderType Type { get; } // Payment/Shipping/Marketplace/AI/Ads/Accounting/Tax
    Task<IntegrationTestResult> TestConnectionAsync(TenantIntegrationCredential credential);
    Task<IntegrationSyncResult> SyncAsync(SyncContext context);
    Task HandleWebhookAsync(WebhookPayload payload);
}
```

### ۱۰.۲ قاعدهٔ سخت (برگرفته از الگوی معیوب دوم در بخش ۲)
هیچ پیاده‌سازی `IIntegrationProvider` مجاز نیست:
- مقدار برگشتی را بدون فراخوانی HTTP واقعی «موفق» اعلام کند؛
- داده‌های نمایشی (نام محصول، قیمت، تصویر) را از کاتالوگ واقعی تننت نگیرد؛
- شکست HTTP را بی‌صدا به موفقیت تبدیل کند.

هر پیاده‌سازی جدید باید یک تست خودکار داشته باشد که با Mock کردن HttpClient روی پاسخ ۵xx/Timeout،
انتظار `IsSuccess=false` را تایید کند — دقیقاً برعکس چیزی که کد مرجع (`SyncStockAndPriceWithDigikala`)
تا پیش از اصلاح داشت.

### ۱۰.۳ صف پس‌زمینه
هر Sync/Publish/Notify از طریق RabbitMQ عبور می‌کند (نه فراخوانی هم‌زمان در Request کاربر)، با
Retry نمایی (Exponential Backoff) و Dead-Letter Queue برای شکست‌های تکراری قابل‌بررسی دستی.

### ۱۰.۴ پوشش سرویس‌ها
بدون تغییر نسبت به سند قبلی (ترب/دیجی‌کالا/دیوار، تاپین/پستکس، سپیدار/راهکاران، سامانهٔ مؤدیان،
دیپ‌فا/آتنا/دیجی‌مارک/ویرا، ترجمیار/فرازین، میزبان‌بات، یکتانت/تپسل، کاوه‌نگار).

---

## ۱۱. سایت مادر، ویزارد و Provisioning

بدون تغییر مفهومی نسبت به سند قبلی (لندینگ → ویزارد ثبت‌نام → پرداخت → `TenantProvisioningService`
→ فعال‌سازی). **افزودهٔ این نسخه:** داشبورد مدیریتی سایت مادر (`MRR`، تعداد تننت، صف SSL) باید
**همیشه** از Query واقعی روی `orders`/`tenantStoreSubscriptions`/`domains` محاسبه شود، هرگز فرمول
تخمینی مبتنی بر شمارش ساده (نمونهٔ اصلاح‌شده: `activeStoresCount * 850000` → `SUM(فقط اشتراک‌های
Active از join واقعی با tenantPlans)`).

---

## ۱۲. فروشگاه تننت، Web API و اپ‌های موبایل

بدون تغییر مفهومی. یک نکتهٔ الزامی جدید: هر Endpoint در لایهٔ Admin API باید از یک Base Controller
مشترک با منطق مجازشماری صریح ارث‌بری کند (نه یک کلاس که در جایی «فرض» شده وجود دارد ولی هرگز
تعریف نشده — دقیقاً مشکلی که با `AuthorizedTenantOwnerApiController` در کد مرجع دیده شد).

---

## ۱۳. دستیار اینستاگرام و کیف‌پول

بدون تغییر مفهومی در قابلیت‌ها (تخفیف در ازای فالو، دایرکت هوشمند، ورود با اینستاگرام، مسابقهٔ
کامنتی). **افزودهٔ الزامی:**
- کیف‌پول اعتباری هر مشتری = `SUM(walletLedger)` (بخش ۶.۵)، هرگز یک عدد پایهٔ فرضی.
- برندهٔ مسابقهٔ کامنتی همیشه از یک **لیست واقعی شرکت‌کنندگان دریافتی از Webhook** با انتخاب
  تصادفی رمزنگارانه (`RandomNumberGenerator`، نه `Random(seed)` قابل‌پیش‌بینی) انتخاب می‌شود.
- حمایت مالی (Donation) از طریق کامنت، هر دو طرف تراکنش (کسر از حامی، واریز به فروشگاه) باید در
  همان تراکنش پایگاه‌داده ثبت شوند؛ محاسبهٔ مبلغ بدون ثبت واقعی (نمونهٔ اصلاح‌شده در بخش ۲) ممنوع است.

---

## ۱۴. استودیوی هوش مصنوعی محتوا

عکس‌برداری استودیویی، ویدیوی کوتاه، صداپیشگی، ترجمهٔ خودکار، سئوی خودکار — همگی به‌صورت
`IIntegrationProvider` نوع AI (بخش ۱۰) پیاده می‌شوند. **قاعدهٔ سخت:** خروجی هرگز از طریق دستکاری
رشتهٔ URL ورودی (مثلاً `.jpg` → `_studio_4k.jpg`) جعل نمی‌شود؛ فایل نهایی باید واقعاً از سرویس AI
دریافت و روی CDN تننت ذخیره شود.

---

## ۱۵. سرویس آموزش (LMS)

### ۱۵.۱ ساختار
محصول از نوع «دوره»، `CourseLessons` (سرفصل + لینک VOD + پیوست)، آزمون چهارگزینه‌ای، گواهی دیجیتال.

### ۱۵.۲ امنیت ویدیو (چندلایه) — بازتعریف دقیق
- میزبانی HLS روی VOD ریالی، بدون فایل مستقیم.
- **توکن پخش امضاشدهٔ HMAC-SHA256** با انقضای زمانی و Bind به IP کاربر + شناسهٔ درس/دوره/مشتری؛
  اعتبارسنجی سمت سرور پیش از هر پخش (این جایگزین دقیق نسخهٔ قبلی است که صرفاً یک GUID تصادفی
  بدون امکان اعتبارسنجی واقعی تولید می‌کرد — رجوع به بخش ۲).
- واترمارک پویا (بخشی از شماره موبایل، ماسک‌شده) روی ویدیوی در حال پخش.
- جلوگیری از اسکرین‌رکورد در اپ (`FLAG_SECURE` اندروید، `isCaptured` iOS).

---

## ۱۶. امنیت و مدیریت کلید/راز

- تمام مقادیر `apiKey`/`apiSecret` در `integrationCredentials` با AES-256 در سطح Application
  رمزنگاری می‌شوند؛ کلید رمزنگاری اصلی در Key Vault/Secret Manager نگهداری می‌شود، هرگز در
  appsettings.json یا کد.
- امضای HMAC توکن‌های ویدیو/API از یک راز مجزا (Rotate‌پذیر) استفاده می‌کند.
- Rate Limiting در سطح Middleware (per-tenant + per-IP) برای جلوگیری از Brute-force روی OTP/ورود.
- تمام Webhook‌های بیرونی (اینستاگرام، دیجی‌کالا، NOWPayments) امضای HMAC/Signature ورودی را
  اعتبارسنجی می‌کنند پیش از پردازش Payload.

---

## ۱۷. رصدپذیری، تاب‌آوری و تست

- **Structured Logging** با `tenantId`/`correlationId` در هر لاگ.
- **Distributed Tracing** (OpenTelemetry) برای پیگیری یک درخواست از Web API تا Worker Consumer.
- **Dead-Letter Queue** قابل‌رصد در پنل ادمین سایت مادر برای Sync‌های شکست‌خورده.
- **تست الزامی برای هر Integration Provider:** حداقل یک تست با Mock پاسخ موفق و یک تست با Mock
  پاسخ ناموفق/Timeout (بخش ۱۰.۲) — این مستقیماً از کمبود مشاهده‌شده در ممیزی کد مرجع می‌آید، جایی
  که هیچ تستی نبود که فاش کند `VerifyPaymentAsync` همیشه موفق برمی‌گرداند.

---

## ۱۸. استانداردهای مهندسی برگرفته از ممیزی کد (الزامی)

این بخش مستقیم‌ترین خروجی ممیزی کد مرجع است و باید در Code Review هر Pull Request بررسی شود:

1. **قاعدهٔ «بدون موفقیت بی‌دلیل»:** هیچ متد Async که یک سرویس بیرونی را نمایندگی می‌کند، مجاز
   نیست بدون فراخوانی HTTP واقعی و بررسی صریح کد وضعیت پاسخ، `true`/`IsSuccess=true` برگرداند.
2. **قاعدهٔ «داده از منبع واقعی»:** هر Feed/گزارش/فید خروجی (RSS، JSON، Dashboard) باید از Query
   واقعی روی مدل داده ساخته شود، نه آرایه‌ای از مقادیر نمونه در کد.
3. **قاعدهٔ «کامپایل قبل از ادعا»:** هیچ Interface/DTO ارجاع‌داده‌شده در یک فایل نباید بدون
   تعریف واقعی در همان Commit باقی بماند؛ CI باید Build را برای هر Pull Request اجرا کند (این
   دقیقاً چیزی بود که در کد مرجع سه بار مشاهده شد: `IFcmService`، `AuthorizedTenantOwnerApiController`،
   `ITenantPlanService`).
3.۱ نبود CI/Build-check دلیل مستقیم بروز مشکل شماره ۳ بود؛ فاز ۱ باید از روز اول یک Pipeline
    داشته باشد که هر Commit را `dotnet build` می‌کند.
4. **قاعدهٔ «تصادفی رمزنگارانه برای هر چیز امنیتی»:** PIN تحویل، انتخاب برندهٔ قرعه‌کشی، هر مقدار
   غیرقابل‌پیش‌بینی امنیتی باید از `RandomNumberGenerator` بیاید، نه `new Random()`.
5. **قاعدهٔ «موجودی محاسبه‌شده، نه ذخیره‌شده»:** هر مقدار مالی جمع‌شونده (کیف‌پول، کمیسیون،
   اعتبار) باید از یک Ledger با جمع Real-time محاسبه شود، نه یک فیلد «موجودی فعلی» که می‌تواند
   به یک عدد پایهٔ فرضی Hardcode بدل شود.
6. **قاعدهٔ «Idempotency روی هر عملیات مالی»:** هر عملیاتی که پول جابه‌جا می‌کند (تایید پرداخت،
   واریز کش‌بک، کسر کیف‌پول) باید یک شناسهٔ یکتا داشته باشد که از اجرای دوبارهٔ ناخواسته جلوگیری کند.

---

## ۱۹. تصمیمات باز قبلی — پاسخ نهایی

| # | موضوع | تصمیم نهایی |
|---|---|---|
| 1 | سطح پیچیدگی موتور تخفیف | **سطح ۲** در فاز ۱، معماری آماده برای سطح ۳ (بخش ۸) |
| 2 | پرداخت ارزی/بین‌المللی | **پیشنهاد فنی: درگاه رمزارزی (NOWPayments/CoinGate)**، مشروط به تایید حقوقی نهایی قبل از فاز ۶ (بخش ۹.۴) |
| 3 | فریمورک فرانت‌اند وب | **Next.js (React)** برای سایت مادر و Storefront (بخش ۳) |
| 4 | صف پیام/Job زمان‌بندی‌شده | **RabbitMQ** (رویدادهای Async) + **Hangfire با Mongo Storage** (Job‌های زمان‌بندی‌شده/تاخیری) — ترکیبی، نه یکی به‌جای دیگری (بخش ۳، ۴) |
| 5 | اولویت‌بندی فازهای ۵ تا ۹ | ترتیب نهایی: **۶ (پرداخت) → ۸ (اینستاگرام) → ۷ (مارکت‌پلیس/لجستیک) → ۹ (AI محتوا) → ۱۰ (LMS) → ۱۱ (حسابداری/مؤدیان)** — منطق در بخش ۲۰ |
| 6 | ساخت موتور سفارش از صفر یا با مرور کد nopCommerce | **از صفر، با مرور کد مرجع اصلاح‌شده (بخش ۲) به‌عنوان راهنمای Edge Case** — بدون کپی، تایید شد |

---

## ۲۰. فازبندی اجرایی نهایی

| فاز | عنوان | خروجی اصلی | وضعیت |
|---|---|---|---|
| 0 | زیرساخت هماهنگی + این سند | این سند؛ تنظیم CI/Build-check از روز اول (بخش ۱۸.۳) | ✅ تکمیل (این سند) |
| 1 | هستهٔ موتور تجارت + چندمستأجری | مدل داده MongoDB (بخش ۶)، Order Engine (بخش ۷)، Discount سطح ۲ (بخش ۸) | ⏳ |
| 2 | سایت مادر + ویزارد + Provisioning + یک درگاه پرداخت تست | بخش ۱۱ | ⏳ |
| 3 | فروشگاه تننت (Storefront) | Next.js Storefront | ⏳ |
| 4 | Web API ادمین + اپ ادمین Flutter | بخش ۱۲ | ⏳ |
| 5 | اپ مشتریان Flutter + Deep Link | بخش ۱۲ | ⏳ |
| 6 | گسترش پرداخت (Parbad کامل + BNPL + رمزارز) + Integration Provider Framework | بخش ۹، ۱۰ | ⏳ |
| 7 | اینستاگرام (طبق اولویت‌بندی جدید، زودتر از مارکت‌پلیس) | بخش ۱۳ | ⏳ |
| 8 | مارکت‌پلیس‌ها + لجستیک | بخش ۱۰.۴ | ⏳ |
| 9 | استودیوی هوش مصنوعی محتوا | بخش ۱۴ | ⏳ |
| 10 | LMS | بخش ۱۵ | ⏳ |
| 11 | حسابداری/مؤدیان + تخفیف سطح ۳ | بخش ۸، ۱۰.۴ | ⏳ |
| 12 | سخت‌سازی امنیتی + راه‌اندازی نهایی | تست نفوذ، بازبینی Multi-Tenancy با کاربر واقعی، استقرار Production | ⏳ |

---

## ۲۲. وضعیت فعلی اجرا (به‌روزرسانی مداوم)

| مورد | وضعیت |
|---|---|
| پلتفرم اجرا | nopCommerce 4.90.6 (سورس رسمی، تایید‌شده در این مخزن) + ۴ پلاگین سفارشی |
| مسیر بومی (.NET 9/MongoDB، بخش‌های ۳ تا ۱۸) | ⏸️ متوقف تا اثبات بازار برند IGBZ |
| سند فنی عملیاتی جاری | `NOPCOMMERCE-EXECUTION.md` (همین مخزن) |
| آخرین ممیزی کد | مقابل سورس واقعی nopCommerce 4.90.6 (نه فقط حدس API) |

**قاعدهٔ حاکم بر این دوره:** تمام قواعد مهندسی بخش ۱۸ (بدون موفقیت بی‌دلیل، داده از منبع واقعی،
کامپایل قبل از ادعا، تصادفی رمزنگارانه، Ledger به‌جای موجودی ذخیره‌شده، Idempotency مالی) **عیناً
روی کد نopCommerce فعلی هم اعمال می‌شود** — این قواعد مختص معماری بومی نبودند، بلکه اصول کیفی
مستقلی هستند که باید در هر پیاده‌سازی، صرف‌نظر از پلتفرم، رعایت شوند.

---

## ۲۳. تاریخچهٔ تصمیمات (Changelog) — به‌روزرسانی

- **نسخهٔ ۳ (این به‌روزرسانی):**
  - تصمیم راهبردی: توقف موقت مهاجرت به پلتفرم بومی؛ ادامهٔ توسعه روی nopCommerce 4.90.6 تا زمان
    بلوغ تجاری برند IGBZ (بخش ابتدای سند).
  - سورس رسمی nopCommerce 4.90.6 در دسترس قرار گرفت؛ تمام فرضیات API نسخهٔ ۲ (نام‌های کلاس،
    امضای متد، الگوی csproj/Migration/View) مقابل سورس واقعی راستی‌آزمایی و اصلاح شد — جزئیات در
    `NOPCOMMERCE-EXECUTION.md`.
  - یک باگ واقعی دیگر در کد مرجع (فراخوانی متد ناموجود `IRepository<T>.TableAsync` در سه‌جای
    `StoreDomainMappingService`) در همین دور کشف و اصلاح شد — نمونهٔ دیگری از این‌که ممیزی بدون
    سورس واقعی همیشه ناقص می‌ماند.

