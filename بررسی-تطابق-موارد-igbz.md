# بررسی تطابق «موارد جدید برای اضافه شدن به IGBZ» با وضعیت واقعی کد

> ⚠️ **نکتهٔ مهم قبل از شروع:** در فایل‌های آپلودی، فایل `راهنمای_راه_اندازی_سرویس_آموزش.txt`
> (مورد ۱۴) در لیست `uploaded_files` بود اما **محتوای آن در پیام شما برایم ارسال/رندر نشده** —
> فقط ۱۱ فایل دیگر را واقعاً دریافت کردم. بخش مربوط به سرویس آموزش را بر اساس آنچه از قبل در
> سند معماری مستند بود بررسی کرده‌ام، نه محتوای دقیق آن فایل. اگر مایلید تحلیل دقیق‌تری روی آن
> فایل داشته باشم، لطفاً دوباره آپلودش کنید.

هر ۱۴ مورد را مستقیماً در کد فعلی (`Plugins/`) جست‌وجو و راستی‌آزمایی کردم، نه فقط بر اساس حافظه.
نتیجه: **بخش قابل‌توجهی از این موارد "سرویس‌سازی" شده‌اند (منطق واقعی نوشته شده)، ولی بخش مهمی
از آن‌ها هرگز به یک نقطهٔ ورود واقعی (Controller/Endpoint/دکمه در پنل ادمین) وصل نشده‌اند** —
یعنی کد وجود دارد ولی هیچ‌کس (نه ادمین، نه یک سرویس بیرونی مثل یکتانت) راهی برای صداکردنش ندارد.

---

## جدول خلاصه

| # | مورد درخواستی | وضعیت | جزئیات |
|---|---|---|---|
| ۱ | AI ساخت پست (تصویر+آهنگ) | 🟡 جزئی | تصویر/ویدیو/صدا هست، «آهنگ» به‌صورت مجزا نیست |
| ۲ | اولویت سرویس‌دهنده داخلی | 🟡 جزئی | در معماری مستند شده؛ در کد Endpointها نمادین‌اند نه برند-محور |
| ۳ | پنل ثبت API Key + راهنما | ✅ کامل (تازه تکمیل شد) | راهنما تازه اضافه شد؛ پایین توضیح داده‌ام |
| ۴ | دسته‌بندی خودکار مسیر ارسال + کد تحویل | ✅ کامل | `LogisticsAndShippingService` |
| ۵ | هماهنگی با دیجی‌کالا/ترب | 🟡 جزئی | منطق کامل، **هیچ Endpoint/Feed عمومی وصل نیست** |
| ۶ | درگاه پرداخت چند‌منظوره | ✅ کامل | `ParbadPaymentService` |
| ۷ | پرداخت ارزی + ترجمه خودکار | 🟡 جزئی | ترجمه **در دیتابیس nopCommerce ذخیره نمی‌شود** |
| ۸ | سئو و تبلیغات | 🟡 جزئی | سئو تولید می‌شود ولی **روی محصول Save نمی‌شود**؛ فید بدون Endpoint |
| ۹ | کدهای تخفیف | ✅ کامل (بومی nopCommerce) | هستهٔ خودِ nopCommerce |
| ۱۰ | Affiliate Marketing | ❌ ناقص جدی | فقط محاسبه؛ نه کد معرف، نه کیف‌پول واقعی، نه داشبورد |
| ۱۲ | کیف‌پول AI مصرفی در اپ مشتری | ❌ پیاده نشده | هیچ‌جا وجود ندارد |
| ۱۳ | افزایش فالوور اینستاگرام | 🟡 جزئی | زیرساخت‌ها هست؛ فلوی مشخص «فالو+منشن» و ورود با اینستاگرام نیست |
| ۱۴ | سرویس آموزش (LMS) | ❌ فقط زیرساخت امنیتی | نه Course، نه Lesson، نه Quiz |

---

## توضیح مورد‌به‌مورد

### ۱. هوش مصنوعی ساخت پست (تصویر AI + آهنگ) — 🟡 جزئی
`AiMultimediaStudioService` سه قابلیت دارد: `EnhanceProductPhotoAsync` (ادیت تصویر)،
`Generate5SecProductVideoStoryAsync` (ویدیوی کوتاه)، `GeneratePersianVoiceOverAsync` (صدا).
**چیزی که نیست:** افزودن موسیقی پس‌زمینه به ویدیو/پست («گذاشتن آهنگ») به‌صورت مجزا پیاده نشده؛
ویدیوی تولیدشده فرض می‌شود خودش صدا/موسیقی دارد، ولی کنترلی روی انتخاب آهنگ وجود ندارد.

### ۲. استفاده از سرویس‌دهنده‌های داخلی — 🟡 جزئی
در سند معماری (بخش ۱۰.۴) و در پنل جدید API Key (نگاه کنید به مورد ۳)، نام‌های واقعی دیپ‌فا/آتنا/
دیجی‌مارک/ویرا/ترجمیار/فرازین/میزبان‌بات ثبت شده‌اند. **اما در خودِ کد سرویس‌ها**
(`AiMultimediaStudioService`, `CryptoAndTranslationService`) آدرس‌های Endpoint هنوز نمادین‌اند
(`api.ai-image-studio-provider.local` و مشابه) — یعنی هیچ‌کدام از این سرویس‌ها هنوز *مشخصاً* به
قالب درخواست/پاسخ واقعیِ دیپ‌فا یا ویرا وصل نشده؛ فقط یک HttpClient واقعی به یک آدرس Placeholder
می‌زند. تا وقتی مستندات API دقیق هرکدام از این سرویس‌ها بررسی نشود، این اتصال Brand-specific نیست.

### ۳. پنل ثبت API Key + راهنما — ✅ (همین الان تکمیل شد)
پنل `/Admin/IntegrationCredentials/Index` از قبل ساخته شده بود (رمزنگاری واقعی، تست اتصال واقعی).
**در همین بررسی متوجه شدم** یک فایل قدیمی و کاملاً جعلی (`ApiManagementController.cs` — که در
اولین دور ممیزی پروژه هم فلگ شده بود ولی هرگز حذف نشده بود) هنوز در پروژه باقی مانده بود؛ یک
لیست کاملاً Hardcode از ۵ سرویس با کلید API قلابی برمی‌گرداند و دکمهٔ Toggle هیچ‌کاری نمی‌کرد.
**حذف شد.** هم‌زمان، ایدهٔ خوبی که همان فایل قدیمی داشت («راهنمای دریافت API Key» به‌ازای هر
سرویس) را به پنل واقعی منتقل کردم: حالا هر Provider یک لینک راهنمای واقعی (مستندات رسمی همان
سرویس) در فرم افزودن/ویرایش نمایش می‌دهد.

### ۴. دسته‌بندی خودکار مسیر ارسال + کد تحویل — ✅ کامل
`LogisticsAndShippingService.CategorizeShipmentRoute` (دسته‌بندی خودکار بر اساس وزن/شهر) +
`GenerateDeliveryPin` (کد تحویل امن) + `RegisterTapinPostShipmentAsync` (ثبت واقعی نزد پیک/پست).
این مورد به‌طور کامل و واقعی (نه جعلی) پیاده‌سازی شده.

### ۵. هماهنگی با دیجی‌کالا و ترب — 🟡 جزئی (نکتهٔ مهم جدید)
`MarketplaceOmnichannelService` منطق واقعی دارد: `GetTorobLiveJsonFeedAsync` (از کاتالوگ واقعی)،
`SyncStockAndPriceWithDigikalaAsync` (HTTP واقعی)، `PublishPostOnKenarDivarAsync`.
**مشکلی که در همین بررسی پیدا کردم:** **هیچ Controller‌ای این سرویس را صدا نمی‌زند.** یعنی:
- هیچ URL عمومی‌ای وجود ندارد که کاربر ترب بتواند در پنل ترب واردش کند (فید هست، ولی بدون در).
- هیچ Schedule Task یا Consumer‌ای Digikala Sync را در لحظهٔ تغییر قیمت/موجودی صدا نمی‌زند.
منطق داخلی درست است، اما **قابل‌استفاده نیست** چون سیمِ اتصال نهایی کشیده نشده.

### ۶. درگاه پرداخت چند‌منظوره — ✅ کامل
`ParbadPaymentService` دقیقاً همان الگوی «چند بانک پشت یک اینترفیس» که سند `درگاه_پرداخت_چند_منظوره`
توصیف کرده را دارد، به همراه دفترکل ضدِ تقلب (`PaymentTransactionLedger`) که حتی از پیشنهاد اولیهٔ
شما هم دقیق‌تر است.

### ۷. پرداخت ارزی + ترجمهٔ خودکار — 🟡 جزئی (نکتهٔ مهم جدید)
`CryptoAndTranslationService.CreateNowPaymentsInvoiceAsync` ✅ واقعی است (دقیقاً طبق راهنمای
NOWPayments شما). اما `AutoTranslateProductCatalogAsync`: متن ترجمه‌شده را از AI می‌گیرد و
**برمی‌گرداند**، ولی — برخلاف چیزی که در فایل راهنمای شما آمده
(`_localizedEntityService.SaveLocalizedValueAsync(...)`) — **هرگز در جدول `LocalizedProperty`
ناپ‌کامرس ذخیره نمی‌شود.** یعنی حتی اگر ادمین دکمهٔ ترجمه را بزند، محصول در نسخهٔ عربی/انگلیسی
سایت هنوز متن فارسی نشان می‌دهد. این یک حلقهٔ ناتمام واقعی است.

### ۸. سئو و تبلیغات — 🟡 جزئی (نکتهٔ مهم جدید)
`SeoAndAdNetworksFeedService.GenerateProductSeoMetaAsync` متن Meta Title/Description تولید
می‌کند اما **مستقیماً روی موجودیت `Product` ذخیره نمی‌شود** (نه `UpdateProductAsync`، نه
`SaveLocalizedValueAsync` برای فیلدهای سئو). دقیقاً مثل مورد ۷، منطق تولید محتوا کامل است ولی
نقطهٔ پایانی «ذخیره در دیتابیس واقعی محصول» نوشته نشده. فید یکتانت هم مثل ترب/دیجی‌کالا (مورد ۵)
هیچ Controller عمومی ندارد.

### ۹. کدهای تخفیف — ✅ کامل (بومی nopCommerce)
این قابلیت جزو هستهٔ خودِ nopCommerce است (`Discount` Entity، کوپن، محدودیت تاریخ/تعداد) و از
قبل کاملاً فعال است؛ نیازی به کدنویسی اضافه نداشت. سطح ۲ (کوپن ساده، بدون ترکیب هم‌زمان) که در
سند معماری به‌عنوان تصمیم فاز ۱ ثبت شده، دقیقاً همین قابلیت پیش‌فرض است.

### ۱۰. Affiliate Marketing — ❌ ناقص جدی (نکتهٔ مهم جدید)
با مقایسهٔ دقیق فایل راهنمای شما (`ffiliate_Marketing.txt`) با کد واقعی:

| چیزی که راهنمای شما خواسته | وضعیت واقعی در کد |
|---|---|
| کد معرف کوتاه و متنی (مثل `ALI100`) | ❌ وجود ندارد |
| ثبت شناسهٔ معرف روی مشتری هنگام ثبت‌نام (Action Filter + کوکی) | ❌ وجود ندارد |
| شنود `OrderPaidEvent` برای واریز خودکار پورسانت | ❌ `OrderPaidEventConsumer` فعلی فقط برای فعال‌سازی اشتراک تننت است، ربطی به Affiliate ندارد |
| واریز واقعی پورسانت به کیف‌پول | ❌ `ProcessAffiliateCommissionAsync` فقط مبلغ را **محاسبه** می‌کند و پیام می‌دهد «صف‌بندی شد» — ولی هیچ رکوردی در `CustomerWalletLedger` (که برای موارد دیگر ساختیم) درج نمی‌کند. این دقیقاً همان الگوی «محاسبه ولی دورریختن» است که چند بار دیگر هم در ممیزی‌های قبلی پیدا و رفع شده بود؛ این‌جا جا مانده بود. |
| داشبورد کاربر (تب در حساب کاربری) | ❌ وجود ندارد |
| پنل ادمین برای تسویه‌حساب | ❌ وجود ندارد |

**جمع‌بندی:** این مورد فقط ۵٪ از چیزی که در تحقیق شما آمده، پیاده شده.

### ۱۲. کیف‌پول هوش مصنوعی مصرفی در اپ مشتری — ❌ پیاده نشده
هیچ کلاس/سرویس/Entity ای با این مضمون («مشتری با هر خرید شارژ می‌شود یا نقدی شارژ می‌کند تا از
دستیار AI تولید محتوا استفاده کند») در پروژه وجود ندارد. آنچه هست (`CustomerWalletLedger` در
`InstagramAssistant`) برای Cashback/حمایت مالی/جایزهٔ مسابقه است، نه اعتبار مصرفیِ ابزار AI. این
یک قابلیت کاملاً جدید است که باید از صفر طراحی شود (schema جدید: `AiUsageCreditLedger` + قوانین
کسر اعتبار به‌ازای هر درخواست AI + مسیر شارژ نقدی از طریق `ParbadPaymentService` موجود).

### ۱۳. افزایش فالوور اینستاگرام — 🟡 جزئی
ساخته‌شده: `InstagramVipAutomationController` (کامنت → لینک خرید در دایرکت)،
`ProductInsertedInstagramConsumer` (پست خودکار محصول جدید)، `LoyaltyWalletAndInstagramContestService`
(قرعه‌کشی واقعی + کیف‌پول). **ساخته‌نشده:** فلوی مشخص «فالو + منشن استوری → کد تخفیف در دایرکت»
(مورد ۱ فایل راهکارهای فالوور شما) به‌عنوان یک Webhook Handler مجزا، و «ورود با حساب اینستاگرام»
(Instagram OAuth Login) که در همان فایل به‌عنوان راهکار ۳ آمده بود.

### ۱۴. راه‌اندازی سرویس آموزش (LMS) — ❌ فقط زیرساخت امنیتی
`ILmsAndVideoSecurityService` (توکن HMAC امضاشده برای پخش امن ویدیو) ساخته و اصلاح شده — این فقط
**یک قطعهٔ زیرساختی امنیتی** است، نه خودِ سرویس آموزش. هیچ‌کدام از این‌ها وجود ندارند: موجودیت
`Course`، `CourseLesson`، `Quiz`، `Certificate`؛ کنترلر ثبت‌نام دوره؛ صفحهٔ پخش دوره در Storefront؛
منطق آزمون/گواهی. (چون محتوای فایل راهنمای مخصوص LMS شما به من نرسید، ممکن است جزئیات دقیق‌تری
در آن فایل باشد که هنوز بررسی نشده — لطفاً دوباره آپلودش کنید.)

---

## پیشنهاد اولویت‌بندی برای جلسهٔ بعدی

با توجه به این‌که همهٔ این خلأها «کار جدید از صفر» نیستند — بیشترشان **تکمیل حلقهٔ ناتمامِ کدی
که از قبل درست نوشته شده** است — پیشنهاد می‌کنم اولویت را این‌طور بچینیم:

1. **وصل‌کردن Endpointهای موجود (مورد ۵ و ۸)** — کم‌هزینه‌ترین کار: فقط یک Controller برای فید ترب/یکتانت و یک دکمه در پنل ادمین محصول لازم است؛ منطق از قبل آماده است.
2. **تکمیل ذخیرهٔ واقعی ترجمه/سئو در دیتابیس (مورد ۷ و ۸)** — افزودن دو فراخوانی (`SaveLocalizedValueAsync`, `UpdateProductAsync`) به سرویس‌های موجود.
3. **Affiliate Marketing واقعی (مورد ۱۰)** — نیاز به کار قابل‌توجه‌تر: تولید کد معرف، Action Filter، اتصال به `CustomerWalletLedger` موجود، داشبورد.
4. **LMS و کیف‌پول AI مصرفی (موارد ۱۲ و ۱۴)** — این دو، پروژه‌های نسبتاً بزرگ و مستقل‌اند؛ نیاز به طراحی Schema جدید از صفر دارند.

کدام‌یک را می‌خواهید اول جلو ببریم؟
