# گزارش ممیزی کد — فهرست کامل مشکلات یافت‌شده و نحوهٔ رفع آن‌ها
## پروژهٔ nopcommerce-plugin-generator (پلاگین‌های مرجع C# + اپ ژنراتور React)

> این سند فهرست دقیق و کامل تمام مشکلاتی است که در ممیزی کد به کد پروژه پیدا شد. مشکلات بر اساس
> شدت (بحرانی / بالا / متوسط) دسته‌بندی شده‌اند. برای هر مورد: فایل، مشکل دقیق، ریسک، و راه‌حلی
> که پیاده‌سازی شد آمده است.

---

## خلاصهٔ آماری

| شدت | تعداد موارد |
|---|---|
| 🔴 بحرانی (Compile نشدن / نشت مالی) | ۷ مورد |
| 🟠 بالا (داده جعلی/گمراه‌کننده به کاربر یا سرویس بیرونی) | ۹ مورد |
| 🟡 متوسط (ضعف امنیتی/کیفی غیرمالی) | ۳ مورد |
| **جمع کل** | **۱۹ مورد** |

---

## 🔴 بخش اول — مشکلات بحرانی

### ۱. `ParbadPaymentService.VerifyPaymentAsync` — همیشه موفق، مستقل از پاسخ بانک
- **فایل:** `Plugins/Nop.Plugin.Misc.MultiTenantStores/Services/ParbadPaymentService.cs`
- **مشکل:** متد تایید پرداخت هیچ فراخوانی واقعی به بانک/PSP انجام نمی‌داد و بدون قید و شرط
  `IsSuccess = true` برمی‌گرداند.
- **ریسک:** هرکسی با صدا زدن مستقیم URL بازگشت (بدون پرداخت واقعی) می‌توانست سفارش را «پرداخت‌شده»
  علامت بزند — دریافت رایگان کالا/خدمات.
- **راه‌حل:**
  - فراخوانی HTTP واقعی به Endpoint تایید PSP.
  - مقایسهٔ دقیق مبلغ تایید‌شده توسط بانک با مبلغ سفارش.
  - افزودن موجودیت جدید `PaymentTransactionLedger` برای ثبت هر تراکنش و جلوگیری از **تایید مضاعف
    (Replay)** — اگر تراکنشی یک‌بار با موفقیت تایید شده باشد، تایید دوباره پول دوباره واریز نمی‌کند.

### ۲. `CryptoAndTranslationService` — «ترجمهٔ محصول» یک رشتهٔ ثابت بود
- **فایل:** `Plugins/Nop.Plugin.Misc.MultiTenantStores/Services/CryptoAndTranslationService.cs`
- **مشکل:** خروجی «ترجمهٔ خودکار» همیشه متن ثابت («Genuine Leather Shoes...») بود، کاملاً مستقل از
  نام/توضیحات محصول ورودی. آدرس کیف‌پول رمزارزی نیز یک رشتهٔ ثابت جعلی بود.
- **ریسک:** به هر فروشنده‌ای، برای هر محصولی، همان متن ترجمهٔ نادرست نمایش داده می‌شد؛ آدرس کیف‌پول
  فرضی می‌توانست باعث گم‌شدن واقعی پول رمزارزی مشتری شود.
- **راه‌حل:** فراخوانی HTTP واقعی به NOWPayments API (برای فاکتور رمزارزی) و یک سرویس ترجمهٔ بیرونی
  (متن واقعی ورودی ارسال و ترجمهٔ واقعی از پاسخ خوانده می‌شود).

### ۳. `MarketplaceOmnichannelService` — فید ترب جعلی + همگام‌سازی دیجی‌کالا بدون تماس شبکه‌ای
- **فایل:** `Plugins/Nop.Plugin.Misc.MultiTenantStores/Services/MarketplaceOmnichannelService.cs`
- **مشکل:**
  - فید JSON ترب فقط شامل **دو محصول نمونهٔ Hardcode شده** بود، مستقل از کاتالوگ واقعی فروشگاه.
  - `SyncStockAndPriceWithDigikalaAsync` بدون هیچ فراخوانی HTTP، همیشه `true` برمی‌گرداند.
  - انتشار آگهی در دیوار هم به همین شکل جعلی بود.
- **ریسک:** ربات‌های قیمت‌یاب مارکت‌پلیس، داده‌ی غلط/قدیمی دریافت می‌کردند؛ صاحب فروشگاه فکر
  می‌کرد موجودی/قیمت در دیجی‌کالا به‌روزرسانی شده در حالی‌که هیچ درخواستی ارسال نشده بود.
- **راه‌حل:** فید ترب اکنون از `IProductService` (کاتالوگ واقعی فروشگاه) ساخته می‌شود؛ همگام‌سازی
  دیجی‌کالا و انتشار در دیوار هر دو یک فراخوانی HTTP واقعی انجام می‌دهند و نتیجه را از کد وضعیت
  پاسخ واقعی می‌خوانند.

### ۴. `SnappPayBnplGateway` — `HttpClient` تزریق‌شده ولی هرگز استفاده نمی‌شد
- **فایل:** `Plugins/Nop.Plugin.Misc.MultiTenantStores/Services/SnappPayBnplGateway.cs`
- **مشکل:** اعتبارسنجی صلاحیت اعتباری مشتری فقط بر اساس محاسبهٔ محلی اقساط بود؛ هیچ استعلامی از
  سرویس واقعی BNPL گرفته نمی‌شد، در حالی‌که `HttpClient` در Constructor تزریق شده بود اما در هیچ‌جای
  کد فراخوانی نمی‌شد (کد مرده).
- **ریسک:** تایید اعتبار اقساطی مشتریانی که واقعاً صلاحیت نداشتند.
- **راه‌حل:** افزودن فراخوانی واقعی HTTP به سرویس استعلام اعتبار پیش از تایید نهایی.

### ۵. `IFcmService` — استفاده می‌شد ولی هیچ‌جا تعریف نشده بود
- **فایل مرتبط:** `Plugins/Nop.Plugin.Api/Controllers/Admin/PushNotificationController.cs`
- **مشکل:** کنترلر Push Notification این Interface را Inject می‌کرد، اما نه Interface و نه هیچ
  کلاس پیاده‌ساز آن در کل مخزن وجود نداشت.
- **ریسک:** کل پلاگین `Nop.Plugin.Api` اصلاً Compile نمی‌شد.
- **راه‌حل:** تعریف کامل `IFcmService`/`FcmService` با یکپارچه‌سازی واقعی Firebase Cloud Messaging
  HTTP v1 API؛ افزوده شدن موجودیت `AdminDeviceToken`؛ شمارش واقعی ارسال‌های موفق/ناموفق به‌ازای هر
  توکن (نه یک عدد ثابت فرضی) + حذف خودکار توکن‌های نامعتبر/منقضی.

### ۶. `AuthorizedTenantOwnerApiController` — کلاس پایهٔ استفاده‌شده در ۴ کنترلر، هرگز تعریف نشده بود
- **فایل‌های متأثر:** `AdminCategoriesController.cs`، `AdminCustomersController.cs`،
  `AdminProductDownloadsController.cs`، `PushNotificationController.cs`
- **مشکل:** هر چهار کنترلر از این کلاس ارث‌بری می‌کردند و متدهای `GetAuthorizedStoreAsync()` و
  `GetCurrentCustomerAsync()` را روی آن صدا می‌زدند، اما این کلاس در هیچ فایلی تعریف نشده بود.
- **ریسک:** کل لایهٔ Admin API قابل کامپایل نبود.
- **راه‌حل:** تعریف کامل کلاس پایه با منطق واقعی احراز هویت/مجازشماری، هماهنگ با فیلترهای امنیتی
  موجود (`TenantAdminScopeFilter`).

### ۷. `ITenantPlanService` — استفاده در ۴ فایل مختلف، هیچ پیاده‌سازی در مخزن نبود
- **فایل‌های متأثر:** `TenantBillingController.cs`، `SubscriptionExpiryScheduleTask.cs`،
  `OrderPaidEventConsumer.cs`، `MasterSiteAdminController.cs`
- **مشکل:** هیچ کلاس یا حتی Interface به نام `ITenantPlanService` در مخزن وجود نداشت، در حالی‌که
  چهار فایل کلیدی (شامل موتور Provisioning و صورتحساب) آن را Inject و صدا می‌زدند.
- **ریسک:** کل جریان صدور فاکتور/تمدید اشتراک/فعال‌سازی خودکار فروشگاه پس از پرداخت، عملاً
  قابل‌اجرا نبود.
- **راه‌حل:** پیاده‌سازی کامل با تمام متدهای مورد نیاز (`GetSubscriptionByStoreIdAsync`,
  `GetPlanByIdAsync`, `CreateSubscriptionOrderAsync`, `GetSubscriptionsExpiringInDaysAsync`,
  `GetExpiredSubscriptionsAsync`, `GetPlanByProductIdAsync`, `GetAllActivePlansAsync`,
  `SendExpiryWarningNotificationAsync`) با اتصال واقعی به Order/Subscription Repository.
  - **مشکل جانبی کشف‌شده:** `TenantBillingController` به فیلدهایی مثل `ExpiryDateUtc`،
    `IsActive`، `AutoRenewEnabled` روی موجودیت `TenantStoreSubscription` ارجاع می‌داد که اصلاً در
    آن موجودیت وجود نداشتند (نام واقعی فیلدها: `NextBillingDateUtc`، `Status`، `AutoRenew` بود).
    این ناهماهنگی هم اصلاح شد.

---

## 🟠 بخش دوم — مشکلات با شدت بالا (داده جعلی/گمراه‌کننده)

### ۸. `ProductPhotoAiStudioService` (در `Nop.Plugin.MultiTenant.Core`) — اصلاً Compile نمی‌شد
- **مشکل:** از انواع `IProductPhotoAiStudioService`، `IAiVisionQualityService`،
  `IAiBackgroundRemovalService`، `PhotoStudioResultDto`، `ProductPhotoRequestDto` استفاده می‌کرد که
  هیچ‌کدام در مخزن تعریف نشده بودند؛ علاوه بر آن، `using`‌های لازم برای `SixLabors.ImageSharp`
  (کتابخانهٔ واترمارک/ادیت تصویر) هم غایب بودند.
- **راه‌حل:** تعریف کامل تمام Interface/DTOهای گمشده + دو سرویس واقعی
  (`AiVisionQualityService`, `AiBackgroundRemovalService` با فراخوانی HTTP واقعی) + افزودن
  `using`‌های گمشده.

### ۹. نسخهٔ تکراری و جعلی همان سرویس در پلاگین `Nop.Plugin.Misc.InstagramAssistant`
- **مشکل:** یک کپی دیگر از `ProductPhotoAiStudioService` در پلاگین اینستاگرام وجود داشت که فقط
  `return true` می‌کرد بدون هیچ پردازش واقعی تصویر — و همزمان با نسخهٔ واقعی/کامل در
  `Nop.Plugin.MultiTenant.Core` ناسازگار بود (دو پیاده‌سازی متفاوت از یک قابلیت در دو پلاگین).
- **راه‌حل:** نسخهٔ جعلی با `[Obsolete(..., error: true)]` علامت‌گذاری شد و ارجاع به نسخهٔ واقعی
  در کامنت مستند گردید تا این ناهماهنگی رفع شود.

### ۱۰. `AiMultimediaStudioService` — خروجی تصویر/ویدیو/صدا با دستکاری رشتهٔ URL جعل می‌شد
- **فایل:** `Plugins/Nop.Plugin.Misc.InstagramAssistant/Services/AiMultimediaStudioService.cs`
- **مشکل:** به‌جای فراخوانی سرویس AI واقعی، کد فقط پسوند/بخشی از URL ورودی را دستکاری می‌کرد
  (مثلاً `.jpg` → `_studio_4k.jpg`) و همان لینک ساختگی را به‌عنوان «تصویر پردازش‌شده» برمی‌گرداند —
  فایلی که هرگز واقعاً وجود نداشت.
- **راه‌حل:** بازنویسی کامل با فراخوانی HTTP واقعی به سرویس‌های AI تصویر/ویدیو/صدا و خواندن URL
  واقعی خروجی از پاسخ سرویس.

### ۱۱. `LoyaltyWalletAndInstagramContestService` — موجودی کیف‌پول یک عدد پایهٔ ثابت فرضی بود
- **مشکل:** موجودی کیف‌پول همیشه با فرمول `150000 + rewardAmount` محاسبه می‌شد — یعنی صرف‌نظر از
  تاریخچهٔ واقعی تراکنش‌های مشتری، همه با یک «کف» ۱۵۰ هزار تومانی فرضی شروع می‌شدند.
- **راه‌حل:** افزودن موجودیت `CustomerWalletLedger` (دفترکل واقعی) و محاسبهٔ موجودی به‌صورت
  `SUM` تمام تراکنش‌های واقعی ثبت‌شده.

### ۱۲. `PickInstagramContestWinnerAsync` — برندهٔ مسابقه یک نام کاربری تصادفی ساختگی بود
- **مشکل:** به‌جای انتخاب واقعی از میان کامنت‌کنندگان/شرکت‌کنندگان واقعی مسابقه، کد یک رشتهٔ فرضی
  مثل `@user_winner_XXX` تولید می‌کرد که هیچ ارتباطی با شرکت‌کنندگان واقعی نداشت.
- **راه‌حل:** بازنویسی متد تا از یک لیست واقعی از شرکت‌کنندگان واجد شرایط (دریافتی از Webhook
  کامنت/منشن اینستاگرام) با انتخاب تصادفی رمزنگارانه (`RandomNumberGenerator`) برنده انتخاب شود.

### ۱۳. `InstagramWalletDonationConsumer` — مبلغ حمایت مالی محاسبه و سپس دور ریخته می‌شد
- **مشکل:** متد مبلغ کامنت («$۵۰» و مشابه) را پارس و محاسبه می‌کرد، اما در انتها فقط
  `await Task.CompletedTask;` اجرا می‌شد — یعنی هیچ کسری از کیف‌پول حامی و هیچ واریزی به فروشگاه
  اتفاق نمی‌افتاد، در حالی‌که کامنت مستندساز کد ادعا می‌کرد این کار انجام می‌شود.
- **راه‌حل:** اتصال واقعی به `ILoyaltyWalletAndInstagramContestService` برای کسر از کیف‌پول حامی و
  واریز واقعی به کیف‌پول صاحب فروشگاه، هر دو با بررسی موجودی کافی حامی پیش از تراکنش.

### ۱۴. `GamificationAndAffiliateService.TriggerAbandonedCartSmsRemindersAsync` — عدد ثابت `۱۲`
- **مشکل:** تعداد سبدهای خرید رهاشده که پیامک یادآوری برایشان ارسال می‌شد، همیشه عدد ثابت `12`
  بود، مستقل از این‌که واقعاً چند سبد رهاشده در پایگاه‌داده وجود داشت.
- **راه‌حل:** Query واقعی روی `ShoppingCartItem` بر اساس آستانهٔ زمانی عدم به‌روزرسانی، شمارش واقعی
  مشتریان یکتا و فراخوانی Callback ارسال پیامک برای هرکدام.

### ۱۵. `SeoAndAdNetworksFeedService.GenerateYektanetRetargetingXmlFeedAsync` — یک آیتم ثابت
- **مشکل:** فید بازاریابی مجدد (Retargeting) یکتانت/تپسل فقط شامل یک محصول نمونهٔ Hardcode شده
  بود، بدون هیچ ارتباطی با کاتالوگ واقعی فروشگاه.
- **راه‌حل:** ساخت فید به‌صورت پویا از `IProductService` (تمام محصولات منتشرشده و قابل‌مشاهدهٔ
  واقعی فروشگاه).

### ۱۶. `MasterSiteAdminController.GetDomainMappings` — سه رکورد نمونه به‌جای داده واقعی
- **مشکل:** با وجود این‌که `IStoreDomainMappingService` واقعی (و کاملاً کاربردی) در Constructor
  تزریق شده بود، متد این سرویس را اصلاً صدا نمی‌زد و به‌جایش سه رکورد دامنهٔ ثابت
  («modstyle.ir»، «beautyshop.com»، «ariacafe.ir») برمی‌گرداند؛ کامنت کد حتی صراحتاً نوشته بود
  «در دیتابیس واقعی از ریپازیتوری خوانده می‌شود» ولی این کار را انجام نمی‌داد.
- **راه‌حل:** افزودن متد `GetAllMappingsAsync` به سرویس واقعی و اتصال کامل کنترلر به آن.

---

## 🟡 بخش سوم — مشکلات با شدت متوسط

### ۱۷. `LmsAndVideoSecurityService` — توکن «امن» ویدیو صرفاً یک GUID تصادفی غیرقابل‌اعتبارسنجی بود
- **مشکل:** «توکن امن پخش ویدیو» فقط بخشی از یک `Guid.NewGuid()` بود، بدون هیچ امضای رمزنگارانه —
  یعنی هرکسی می‌توانست توکن خودش را جعل کند، چون هیچ مکانیزم اعتبارسنجی واقعی وجود نداشت.
- **راه‌حل:** پیاده‌سازی توکن امضاشدهٔ **HMAC-SHA256** با انقضای زمانی، مقید به IP کاربر و شناسهٔ
  دوره/درس/مشتری + متد `ValidateSignedToken` برای بررسی واقعی امضا پیش از پخش.

### ۱۸. `LogisticsAndShippingService.GenerateDeliveryPin` — PIN تحویل با `Random(seed)` قابل‌پیش‌بینی
- **مشکل:** کد تحویل (OTP) از `new Random(orderId + میلی‌ثانیه)` تولید می‌شد — یک تصادفی‌سازی
  ضعیف و از‌نظر تئوری قابل‌حدس‌زدن؛ علاوه بر آن، کد رهگیری پستی هم به‌جای فراخوانی واقعی API
  تاپین، به‌صورت محلی ساخته می‌شد.
- **راه‌حل:** تولید PIN با `RandomNumberGenerator` (رمزنگارانه)، و ثبت واقعی مرسوله از طریق
  فراخوانی HTTP به API تاپین با خواندن کد رهگیری واقعی از پاسخ.

### ۱۹. `MasterSiteAdminController.GetDashboardSummary` / `VerifyDomainSsl` — آمار و تاییدیهٔ SSL جعلی
- **مشکل:**
  - `MonthlyRecurringRevenueToman` با فرمول `تعداد_فروشگاه × ۸۵۰۰۰۰` تخمین زده می‌شد (فرض غلط
    این‌که همهٔ تننت‌ها یک نرخ ثابت دارند).
  - `PendingCustomDomainsSsl`، `StorageUsedGb`، `DatabaseSizeMb` همگی اعداد ثابت Hardcode بودند.
  - `VerifyDomainSsl` بدون هیچ بررسی واقعی رکورد DNS/CNAME، همیشه پیام موفقیت برمی‌گرداند.
- **راه‌حل:** محاسبهٔ MRR واقعی از Join بین اشتراک‌های فعال و قیمت واقعی پلن‌ها؛ شمارش واقعی
  دامنه‌های در انتظار SSL از ریپازیتوری؛ اتصال `VerifyDomainSsl` به متد واقعی
  `VerifyCustomDomainCnameAsync` پیش از علامت‌زدن دامنه به‌عنوان تاییدشده.

---

## جمع‌بندی الگوی مشترک

تقریباً تمام مشکلات بالا از یک الگوی تکرارشونده ناشی می‌شدند: **«فراخوانی/محاسبهٔ واقعی حذف و با
یک مقدار ثابت یا فرمول ساده جایگزین شده بود»** — گاهی با کامنتی که صراحتاً ادعای پیاده‌سازی واقعی
داشت (نمونهٔ بند ۱۶). دو علت ریشه‌ای اصلی شناسایی شد:

1. **نبود لایهٔ ماندگاری (Ledger) برای مقادیر انباشتی** (کیف‌پول، تراکنش پرداخت) — که باعث می‌شد
   جای این مقادیر واقعی، یک عدد پایهٔ فرضی جایگزین شود.
2. **نبود بررسی Build/Compile خودکار (CI)** — که اجازه می‌داد فایل‌هایی با ارجاع به Interface/کلاس
   ناموجود (`IFcmService`, `AuthorizedTenantOwnerApiController`, `ITenantPlanService`) بدون خطا در
   مخزن باقی بمانند.

هر دو علت در سند معماری جدید (`ARCHITECTURE-NATIVE-v2.md`، بخش‌های ۶ و ۱۸) به‌صورت قاعدهٔ الزامی
برای جلوگیری از تکرار در پیاده‌سازی بومی جدید مستند شده‌اند.
